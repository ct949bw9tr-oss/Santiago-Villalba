{
  "name": "@taskswift/seed-data",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured for seed-data'",
    "test": "echo 'no tests for seed-data'"
  },
  "dependencies": {
    "@taskswift/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
