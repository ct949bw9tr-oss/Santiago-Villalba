{
  "name": "@taskswift/config",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured for config'",
    "test": "echo 'no tests for config'"
  },
  "dependencies": {
    "@taskswift/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
