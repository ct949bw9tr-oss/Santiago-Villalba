{
  "name": "@taskswift/types",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured for types'",
    "test": "echo 'no tests for types'"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
